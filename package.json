{
  "name": "apra-pm",
  "version": "0.3.5",
  "private": true,
  "description": "Provider-agnostic Project Manager skill: one orchestrator session drives planner/plan-reviewer/doer/reviewer subagents across parallel git worktrees, with a planner-chosen model per task. No MCP, no server.",
  "type": "module",
  "bin": {
    "pm-install": "install.mjs"
  },
  "scripts": {
    "install-skill": "node install.mjs",
    "test": "node --test test/**/*.test.mjs",
    "e2e": "node e2e/run-e2e.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "ajv": "^8.20.0"
  }
}
