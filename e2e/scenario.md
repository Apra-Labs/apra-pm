# pm e2e -- sprint on the toy

Use the **pm** skill to run one sprint. Inputs the skill needs:

- **Repo:** already cloned at `{{REPO}}` (base `main`, remote `origin` -> the toy).
- **Branch:** `{{BRANCH}}`.
- **Requirement:** implement these three P1 issues: `gh-toy-mi2` (CLI CRUD commands), `gh-toy-7rp` (help system and input validation), `gh-toy-13t` (input validation for blank strings).

Then run the pm commands in order: **plan** (with those 3 issues as the
requirement), **start**, **cleanup**.
