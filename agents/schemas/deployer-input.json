{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "apra-pm/deployer-input@1",
  "version": 1,
  "title": "deployer input",
  "description": "Required dispatch-context keys for the deployer role, per agents/deployer.md Inputs section. Consumed by callers for pre-dispatch validation only -- never appended to the prompt or shown to the LLM.",
  "type": "object",
  "required": ["operation", "repoRoot"],
  "properties": {
    "operation": {
      "type": "string",
      "enum": ["deploy"]
    },
    "repoRoot": {
      "type": "string",
      "description": "Repo root path where deploy.md lives."
    }
  }
}
