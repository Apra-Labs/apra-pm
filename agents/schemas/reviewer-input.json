{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "apra-pm/reviewer-input@2",
  "version": 2,
  "title": "reviewer input",
  "description": "Required dispatch-context keys for the reviewer role, per agents/reviewer.md Inputs section. Consumed by callers for pre-dispatch validation only -- never appended to the prompt or shown to the LLM.",
  "type": "object",
  "required": ["base-branch", "branch", "beadIds"],
  "properties": {
    "base-branch": {
      "type": "string",
      "description": "Branch to diff against, e.g. \"main\"."
    },
    "branch": {
      "type": "string",
      "description": "Sprint track branch to review."
    },
    "beadIds": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "The exact bead ids just worked -- the reviewer's entire review list. It never scans 'bd list --status=closed' to find recent work."
    }
  }
}
